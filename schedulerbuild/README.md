#Backend Scheduler Build Project
###Helpful Maven Commands

run project from maven console
```
mvn spring-boot:run
```

compile .jar to "target" directory in root directory of project
```
mvn clean install
```
